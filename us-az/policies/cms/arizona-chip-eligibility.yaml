format: rulespec/v1
module:
  status: deferred
  proof_validation:
    required: true
  summary: |-
    This Arizona module is deferred in this repository state because its previously declared primary source text is not present in the pinned corpus snapshot. Executable rules must be restored only after the source is ingested into corpus.
  deferred_outputs:
    - output: us-az:policies/cms/arizona-chip-eligibility#arizona_separate_chip_child_eligibility_available
      reason: Requires the primary source text to be present in the pinned corpus snapshot before executable grounding can be restored.
    - output: us-az:policies/cms/arizona-chip-eligibility#is_chip_eligible_child
      reason: Requires the primary source text to be present in the pinned corpus snapshot before executable grounding can be restored.
    - output: us-az:policies/cms/arizona-chip-eligibility#arizona_standard_pregnant_chip_eligibility_available
      reason: Requires the primary source text to be present in the pinned corpus snapshot before executable grounding can be restored.
    - output: us-az:policies/cms/arizona-chip-eligibility#is_chip_eligible_standard_pregnant_person
      reason: Requires the primary source text to be present in the pinned corpus snapshot before executable grounding can be restored.
rules: []
