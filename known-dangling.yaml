# Scope entries known not to resolve in this repository, tracked in
# https://github.com/TheAxiomFoundation/axiom-programs/issues/14.
# tests/test_program_specs.py fails on dangling entries NOT listed here,
# and on listed entries that start resolving (remove them when fixed).
# Do not add entries without an issue link.
#
# 2026-07-06: the us-ma/snap entries were resolved — 365/180 is imported
# as block-1 (its encoded form), and the unencoded DTA FY 2026 COLA SUA
# scope entry is commented out in the spec until the module is promoted
# (see programs/us-ma/snap/fy-2026.yaml).
entries: []
