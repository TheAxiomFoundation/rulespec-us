# PR #878 author remediation

The Mississippi module cannot be accepted or honestly attested in this checkout yet.

1. Publish a new immutable `axiom-corpus` release containing scope
   `us-ms/manual/2026-05-27-ms-snap-manual`; do not mutate or rebind
   `us-rulespec-2026-07-13`.
2. Repin rulespec-us to that new release through the migration protocol, keeping
   the existing three-key toolchain pin coherent.
3. From a clean checkout at the final PR head, use the toolchain-pinned,
   reviewed `axiom-encode` signer and the real signing key to run
   `axiom-encode sign-applied-files --repo . --base-ref origin/main --head-ref HEAD`
   for these exact files:
   - `us-ms/manual/mdhs/snap-policy-manual/part-14/page-102.yaml`
   - `us-ms/manual/mdhs/snap-policy-manual/part-14/page-102.test.yaml`
4. Commit the generated manifest under `.axiom/encoding-manifests/`, verify its
   `applied_files` hashes match both files, and verify the signed commit remains
   an ancestor of the final PR head. Do not hand-edit or fabricate the manifest
   or signature.
5. Run repository layout, encoding-manifest, companion-test, and Mississippi
   module validation before requesting review again.

