# PR #882 author remediation

The provenance/toolchain surface must land separately from the bulk feature.

The fix commit on `fix/882` removes the feature PR's changes to the protected
workflow files. A separate, explicitly coordinated provenance/toolchain PR must
carry any intended:

- `oracle-coverage-axiom-encode-ref` change;
- entry-level encoder override and approval of encoder commit
  `9978ad8181914affdebcd6fb881291198c3bc839`;
- `attest_ref` workflow input and repaired-branch re-attestation job;
- signer checkout, signing-key handling, or signed-manifest regeneration flow.

That separate PR must be reviewed against all three pinned toolchain keys and
all migrated consumers. Only after it lands may this feature branch be rebased
and any affected manifests be regenerated by the real reviewed signer. Do not
copy, synthesize, or hand-edit signatures in this branch.
